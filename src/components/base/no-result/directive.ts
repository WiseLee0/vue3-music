import noResult from './no-result.vue';
import { createLikeLoadingDirective } from '@/assets/js/directive'

const noResultDirective = createLikeLoadingDirective(noResult)

export default noResultDirective